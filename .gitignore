# === Datos confidenciales (NO subir a un repo público) ===
*.xlsx
*.xls
backend/tmp/
backend/app/data/*.xlsx

# === Python / backend ===
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# === Node / frontend ===
node_modules/
dist/
dist-ssr/
*.local

# === Entorno ===
# Se ignoran los .env locales/secretos, pero NO .env.production (necesario
# para el build de Fly: contiene la URL pública del backend, no secretos).
.env
.env.local
.env.*.local
.DS_Store

# === Editores ===
.vscode/
.idea/
